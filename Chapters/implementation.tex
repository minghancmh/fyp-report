\chapter{Implementation}
In this chapter, we will delve deeper and take a closer look at the implementation details of \texttt{cloudkube}. This chapter is organized into several sections.

Section 4.1 will discuss the architecture of \texttt{cloudkube}, where we will see a high level overview of the different modules and services in \texttt{cloudkube}.
We will also see the detailed workflow of \texttt{cloudkube} via the CLI, which will allow us to see how the different modules interact with one another.

Next, section 4.2 will discuss system design patterns used for the implementation of \texttt{cloudkube}. We will rationalise these choices, and explore how these
patterns are realised in \texttt{cloudkube}.

Section 4.3 will delve deeper into each of the different modules in \texttt{cloudkube}, 
where we will look at detailed code segments to see how the different functions are implemented.

Lastly, section 4.4 will cover the publishing of \texttt{cloudkube} to \texttt{pip} for easy distribution,
as well as the development of github workflows for publishing and realising to \texttt{pip}.

\section{\texttt{cloudkube} Architecture} 
\begin{figure}[h!]
    \centering
    \includegraphics[width=\textwidth]{fig/cloudkube-architecture.png}
    \caption{\texttt{cloudkube} architecture diagram}
    \label{fig:cloudkube-architecture-diagram}
\end{figure}


\texttt{cloudkube} is an orchestration tool designed to simplify the development and management of Kubernetes clusters in cloud environments. It leverages terraform
for infrastructure provisioning, and provides python scripts for managing the various cloud components. These python scripts are made available thorugh a simple CLI interface,
which will be detailed in a later section. It should be noted that the primary objective of this project was to simplify infrastructure provisioning. Consequently, the
focus was placed on designing and implementing robust cloud infrastructure, while the user interface was intentionally kept simple. In order to streamline interactions, \texttt{cloudkube}
features a straighforward CLI, ensuring functionality and ease of use without unnecessary complexity.

\subsection{Configuring the cluster}
As we see from Figure \ref{fig:cloudkube-architecture-diagram}, the main entry point into the system is through the \texttt{cloudkube} CLI. This entry point (Figure \ref{fig:cloudkube-init}) can be accessed
through the command \texttt{cloudkube init}.

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.8\textwidth]{fig/cloudkube-init.png}
    \caption{\texttt{cloudkube} entry point}
    \label{fig:cloudkube-init}
\end{figure}



Following this, the first step would then be to select Option 2, where control is handed over to the \texttt{Cluster Configuration Manager}. The corresponding 
UI is show in Figure \ref{fig:cloudkube-config}. At this stage, the \texttt{Cluster Configuration Manager} asks us for the following input: 
\vspace{-1em}
\begin{itemize}
    \item \textbf{Region:} This refers to the region in the cloud where we wish to provision our resources.
    \item \textbf{Prefix:} This allows us to set a unique prefix, for easy identification of our cloud resources created. This is especially useful for working in
    large teams, where cloud environments have many resources provisioned.
    \item \textbf{Architecture of Machines:} This allows us to choose between the two major types of machine architectures, \texttt{x86} and \texttt{arm}.
    \item \textbf{Ingress Controller Types:} This allows us to choose whether we need an ingress controller, as well axs the type of ingress controller to use. If selected,
    this ultimately provisions an load balancer in the cloud, which satisfies Kubernetes \texttt{Ingress} resources.
    \item \textbf{Persistent Volume Setup:} This allows us to choose whether we need File System setup, which ultimately satisfies Kubernetes \texttt{Persistent Volume}
    resources. 
\end{itemize}


\begin{figure}[h!]
    \centering
    \includegraphics[width=0.8\textwidth]{fig/cloudkube-config.png}
    \caption{\texttt{cloudkube} cluster configuration}
    \label{fig:cloudkube-config}
\end{figure}

\begin{figure}[h!]
    \centering \includegraphics[width=0.8\textwidth]{fig/cloudkube-config-success.png}
    \caption{\texttt{cloudkube} cluster configuration success}
    \label{fig:cloudkube-config-success}
\end{figure}

\begin{figure}[h!]
    \centering \includegraphics[width=0.8\textwidth]{fig/cloudkube-config-cwd.png}
    \caption{\texttt{cloudkube} \texttt{config.json} saved to our current working directory}
    \label{fig:cloudkube-config-cwd}
\end{figure}

\begin{figure}[h!]
    \centering \includegraphics[width=0.8\textwidth]{fig/cat-config.png}
    \caption{Contents of \texttt{config.json}}
    \label{fig:cat-config}
\end{figure}

At each of these steps, the \texttt{Cluster Configuration Manager} validates our input. Once our input is verified, the \texttt{Cluster Configuration Manager}
shows the corresponding configuration file that it has saved to \texttt{config.json} (Figure \ref{fig:cloudkube-config-success}). 
It should be noted that thie \texttt{config.json} file is saved to our current working directory, which is where we ran \texttt{cloudkube init} from. 

In the previous step,
we ran \texttt{cloudkube init} in our \texttt{FYP Report} directory, and Figure \ref{fig:cloudkube-config-cwd} shows the \texttt{config.json} in our \texttt{FYP Report}
directory. Figure \ref{fig:cat-config} shows the contents of \texttt{config.json}, which is output by the \texttt{Cluster Configuration Manager}.

At this point, we have entered all the configuration that \texttt{cloudkube} requires, and \texttt{cloudkube} is ready to provision our desired infrastructure 
for us (Figure \ref{fig:cloudkube-config-success}). 

\subsection{Provisioning the Infrastructure}
\begin{figure}[h!]
    \centering \includegraphics[width=0.8\textwidth]{fig/cloudkube-provision.png}
    \caption{Truncated output of terraform plan}
    \label{fig:cloudkube-provision}
\end{figure}

\begin{figure}[h!]
    \centering \includegraphics[width=0.8\textwidth]{fig/cloudkube-provision-approval.png}
    \caption{User input for approval of terraform plan}
    \label{fig:cloudkube-provision-approval}
\end{figure}

In order to spin up our infrastructure, we will then select Option 3 (Figure \ref{fig:cloudkube-provision}). \texttt{cloudkube} then hands over 
control to the \texttt{Cluster Manager}. The \texttt{Cluster Manager} first reads from the configuration file \texttt{config.json} that 
we configured earlier. It then uses these options to provision the corresponding infrastructure on the cloud. The \texttt{Cluster Manager} 
will then output the plan from \texttt{terraform} for user approval (Figure \ref{fig:cloudkube-provision-approval}). The full output is attached in Appendix \ref{appendix:cloudkube-provision-tf-plan}. 
Upon user approval, the plan is then applied to the cloud environment. Any errors, info, or warning logs output by terraform are 
reflected into the \texttt{cloudkube} CLI for easier debugging by the user.

\begin{figure}[h!]
    \centering \includegraphics[width=0.8\textwidth]{fig/cloudkube-output.png}
    \caption{truncated \texttt{cloudkube} output upon successful provisioning}
    \label{fig:cloudkube-output}
\end{figure}

Once the terraform plan has been successfully applied to the cloud, relevant information about the cluster, as well as info logs are 
output by \texttt{cloudkube} (Figure \ref{fig:cloudkube-output}). The full output by \texttt{cloudkube} upon successful infrastructure
provisioning is presented in Appendix \ref{appendix:cloudkube-successful-provision-output}.

\begin{lstlisting}[caption={truncated \texttt{cloudkube} output upon successful provisioning}, breaklines=true, breakatwhitespace=false, numbers=left, label={lst:cloudkube-success-output}, xleftmargin=10pt]
    Outputs:

cluster_endpoint = "https://5862339F7E77382B0A74D8C85BD42D31.gr7.ap-southeast-1.eks.amazonaws.com"
cluster_name = "mh-fyp-eks-wFMZsm5Z"
cluster_security_group_id = "sg-0869b037f781256c0"
efs_id = "fs-078a7048b2c301c27"
region = "ap-southeast-1"
[INFO]: Terraform applied successfully
Raw Terraform output for 'cluster_name': mh-fyp-eks-wFMZsm5Z
Raw Terraform output for 'region': ap-southeast-1
region:ap-southeast-1, cluster_name:mh-fyp-eks-wFMZsm5Z
[INFO]: kubectl configured successfully
[INFO]: install_aws_nginx_controller: Applied resource from https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.12.0-beta.0/deploy/static/provider/aws/deploy.yaml successfully
\end{lstlisting}

Referring to Listing \ref{lst:cloudkube-success-output}, we see at line 8 that the application of the terraform plan has completed
successfully. The corresponding outputs, including the provisioned EFS that we configured previously (Figure \ref{fig:cat-config}), are shown.
These outputs can then be used in our Kubernetes manifests.

At line 12 (Listing \ref{lst:cloudkube-success-output}), we see the log \texttt{kubectl configured successfully}. Under the hood,
\texttt{cloudkube} authenticates the local environment with the Kubernetes cluster provisioned in the cloud. This populates the 
\texttt{\textasciitilde/.kube/config} file, which allows us to communicate with the Kubernetes cluster from our local environment. 

Lastly, given that we required an ingress controller for our set up (Figure \ref{fig:cat-config}), \texttt{cloudkube} provisions an
ingress controller, and logs the successfully provisioning of this ingress controller at line 13 (Listing \ref{lst:cloudkube-success-output}).

At this stage, we have a fully functional Kubernetes cluster up and running in the cloud of our choice. We can then interact with the cluster,
and deploy our application to the cluster. In the subsequent chapter, we will look at the deployment of two types of applications
to this cluster. 



\section{System Design Patterns}
The implementation of several system design patterns in \texttt{cloudkube} was instrumental in ensuring the system
was both modular and scalable. In particular, the Command pattern helped to encapsulate user actions such as provisioning and tearing
down resources into discrete, reusable commands. On the other hand, the Facade pattern was also employed to abstract the 
complexities of underlying operation, which helped to simplify the user experience, ensuring seamless interaction with \texttt{cloudkube}
without requiring knowledge of its internal intricacies. 

\subsection{Command Pattern}
The Command pattern is used to encapsulate requests as objects, which allow to parametrization of clients
with different requests \cite{gamma1994design}. This allows for loose coupling between commmand objects as well
as the invoker. Specific to \texttt{cloudkube}, the command pattern is seen in the menu-driven system \texttt{run\_wizard}.

\begin{lstlisting}[xleftmargin=10pt, caption={Code snippet for main \texttt{cloudkube} entrypoint}]
def run_wizard():
    while True:
        display_menu_options()
        selection = input("Selection: ")
        print()
        match selection:
            case "1":
                clusterConfig.configure_cloud()
            case "2":
                clusterConfig.configure_cluster_interactive()
            case "3":
                ClusterManager.spin_up_cluster()
            case "4":
                ClusterManager.tear_down_cluster()
            case "5":
                print("exiting...")
                break
            case _:
                print("Invalid option selected. Please re-enter.")

\end{lstlisting}

As seen in the code snippet above, the \texttt{match} statement in \texttt{run\_wizard} maps the user input to
specific operations. Each option then corresponds to a command, that is encapsulated as a function all by a controller 
component. This allows the easy extension of a specific action, allowing for easy addition of new commands without 
drastic changes to the core application logic. 


\begin{figure}[h!]
    \centering
    \includegraphics[width=0.5\textwidth]{fig/commands-package.png}
    \caption{Commands Module}
    \label{fig:commands-module}
\end{figure}

While the individual commands in \texttt{cloudkube} are not explicit Classes, they are abstracted to a package within \texttt{cloudkube} (Figure \ref{fig:commands-package}).
Each of these command objects perform specific actions, and are call upon by the controllers (i.e. \texttt{Cluster Manager}). It should also
be noted that each of these commands come with a corresponding \textit{undo} command. For example, the \texttt{install\_efs\_csi\_driver} has a 
corresponding \texttt{remove\_efs\_csi\_driver}. As we will see later on, this makes it easy to make calls directly to the \texttt{cloudkube} CLI to modify cloud configuration.


\subsection{Facade Pattern}
The Facade pattern helps to provide a unified interface, which makes the system easier to use \cite{gamma1994design}. In the case of \texttt{cloudkube},
the \texttt{Cluster Manager} and \texttt{Cluster Configuration Manager} modules serve as facades for more complex underlying objects.
They help to provide a simplified, high-level interface for cluster configuration, as well as spinning up and tearing down clusters.

\begin{lstlisting}[xleftmargin=10pt, caption={Code snippet for ClusterManager class method}]
    def spin_up_cluster(path_to_config="config.json"):
        cfg = ClusterManager.__parse_config(path_to_config)
        if not cfg:
            print(
                "Invalid configuration file provided. Have you run '2. Configure cluster configuration?'"
            )
            return

        # Step 1: Apply Terraform
        cluster_name, region = apply_terraform()

        # Step 2: Configure kubectl for the EKS cluster
        configure_kubectl(cluster_name, region)

        # region, prefix, architecture are used in the main.tf terraform code

        # at this step, we only need to check ingress_controller and require_efs state

        if cfg["ingress_controller"] != "None":
            install_ingress_controller(cfg["ingress_controller"])

        if cfg["require_efs"] == "True":
            apply_efs_csi_driver(
                "kubernetes-sigs/aws-efs-csi-driver", ref="release-2.0"
            )

\end{lstlisting}

For example, in the code snipped above for the class method \texttt{spin\_up\_cluster}, we see a grouping of multiple 
lower-level operations such as \texttt{apply\_terraform}, and \texttt{configure\_kubectl}. This abstracts away the intricate details
when provisioning the cluster, making the code more readable, and navigatable when understading the underlying logic in \texttt{cloudkube}. 

\section{Module Implementations}
\subsection{commands module}
\begin{figure}[h!]
    \centering
    \includegraphics[width=0.5\textwidth]{fig/commands-package.png}
    \caption{Commands Module}
    \label{fig:commands-package}
\end{figure}
The commands module is structured to adhere to the Command Pattern as discussed earlier. Each command is encapsulated as an individual command object. This design ensures that each
operation, such as configuring \texttt{kubectl}, or installing the EFS CSI driver is modular and self-contained. By adopting this pattern, this module provides ease of extensibility,
allowing new commands to be added without modifying existing implementations, or core application logic. This modular approach is particulary beneficial for \texttt{cloudkube},
enabling seamless integration of additional cloud-native workflows while maintaining a clean separation of concerns. Each script within the commands module serves a distinct purpose, making it easier
to support flexible deployment strategies. It should also be noted that every command has a corresponding "reverse" command. This symmetrical command structure (Table \ref{tab:commands}) enhances reversibility and idempotency as it
allows \texttt{cloudkube} to gracefully rollback configurations if needed. 

\begin{table}[h]
    \centering
    \begin{tabular}{|p{4cm}|p{4cm}|p{6cm}|}
        \hline
        \textbf{Command} & \textbf{Reverse Command} & \textbf{Responsibility} \\
        \hline
        configure\_kubectl.py & unconfigure\_kubectl.py & Configuration/Cleanup of the local environment to interact with the cluster (\~/.kube/config) \\
        \hline
        install\_efs\_csi\_driver.py & remove\_efs\_csi\_driver.py & Installs/Uninstalls the EFS CSI driver in the cluster for persistent storage. \\
        \hline
        install\_ingress.py & remove\_ingress.py & Deploys/Removes an Ingress controller for managing external access to cluster services. \\
        \hline
    \end{tabular}
    \caption{\texttt{cloudkube} Commands and Their Reverse Actions}
    \label{tab:commands}
\end{table}



\subsection{terraform module}
The terraform module hosts all of the terraform code used to spin up the infrastructure. This package is distributed along \texttt{cloudkube}
to allow for easy installation on any device with a python environment. 

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.5\textwidth]{fig/terraform-module.png}
    \caption{Terraform module directory structure}
    \label{fig:terraform-module}
\end{figure}
The three main files that will be explained in detail in this subsection are the \texttt{main.tf}, \texttt{terraform.tf}, and the 
\texttt{outputs.tf} file. The remaining files/folders are autogenerated by terraform. Briefly, they are
\begin{enumerate}
    \item \texttt{.terraform} folder: This is an internal directory created by Terraform automatically during the run time of \texttt{terraform init}. It is used to store essential Terraform metadata, downloaded provier plugins, backend configurations, and state lock files. 
    \item \texttt{.terraform.lock.hcl}: This file is a dependency lock file that Terraform use to ensure consistent provider versions across different environments and machines. This prevents unexpected updates which could cause compatibility issues.
    \item \texttt{terraform.tfstate}: This file helps to track the state of the infrastructure. It contains information about the resources created by Terraform, their current configurations, and mappings between Terraform configurations and real-world infrastructure.
    \item \texttt{terraform.tfstate.backup}: This file is an automatic backup of the previous state before Terraform applies changes. It ensures that the infrastructure can be restored to the last known good state if anything goes wrong.
\end{enumerate}

\textbf{\texttt{terraform.tf}}

This file houses the \texttt{terraform} block which is essential for defining the cloud providers. In the context of \texttt{cloudkube}, 
the sole cloud provider that is used is \texttt{aws}. The \texttt{random} provider is used to generate a random prefix string to 
prepend to all of our cloud resources for easy identification, as well as reduce probability of clashes. Lastly, the \texttt{tls} module
is included to generate and manage TLS certificates, private keys, and self-signed certificates, to aid in provisioning cloud resources
requiring encryption.


\begin{lstlisting}[xleftmargin=10pt, caption={\texttt{terraform.tf} source code}]
terraform {

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.47.0"
    }

    random = {
      source  = "hashicorp/random"
      version = "~> 3.6.1"
    }

    tls = {
      source  = "hashicorp/tls"
      version = "~> 4.0.5"
    }
  }

  required_version = "~> 1.3"
}
\end{lstlisting}

\texttt{outputs.tf}

The \texttt{outputs.tf} file determines the output shown after running the command to provision the infrastructure. In particular, 
the \texttt{efs\_id} should be incorporated into the Kubernetes manifest when defining the persistent volume (Line 13 of Listing \ref{lst:kubernetes-pv}). 
The remaining outputs are used by the \texttt{boto3} (AWS Cloud SDK) module to configure the local environment to communicate with the 
cluster.

\begin{lstlisting}[xleftmargin=10pt, caption={\texttt{outputs.tf} source code}]
output "cluster_endpoint" {
  description = "Endpoint for EKS control plane"
  value       = module.eks.cluster_endpoint
}

output "cluster_security_group_id" {
  description = "Security group ids attached to the cluster control plane"
  value       = module.eks.cluster_security_group_id
}

output "region" {
  description = "AWS region"
  value       = local.config.region
}

output "cluster_name" {
  description = "Kubernetes Cluster Name"
  value       = module.eks.cluster_name
}

output "efs_id" {
  description = "Persistent Volume EFS ID"
  value       = aws_efs_file_system.efs.id
}

\end{lstlisting}

\begin{lstlisting}[xleftmargin=10pt, caption={Definition of Kubernetes Persistent Volume for ASR Application}, label={lst:kubernetes-pv}]
apiVersion: v1
kind: PersistentVolume
metadata:
    name: model-storage
spec:
    capacity:
        storage: 5Gi
    accessModes:
        - ReadWriteMany # EFS supports multi-node access
    storageClassName: efs-sc
csi:
    driver: efs.csi.aws.com
    volumeHandle: fs-015b986c551bc3640
\end{lstlisting}

\texttt{main.tf}

The \texttt{main.tf} file houses all the main terraform code that is used to provision the Kubernetes cluster in the AWS Cloud. 

\begin{lstlisting}[xleftmargin=10pt, caption={Parsing \texttt{config.json} as locals in \texttt{main.tf}}, label={lst:json-tf}]
variable "config_file" {
    description = "Path to the JSON configuration file"
    type        = string
    default     = "" # Provide a default if applicable, or leave it empty
}
    
locals {
    config = jsondecode(file(var.config_file))
}
\end{lstlisting}

Firstly, we will parse the \texttt{config.json} file which is automatically generated by \texttt{cloudkube} to determine the 
type of infrastructure to spin up. The generation of the \texttt{config.json} file will be further elaborated on in the subsection on 
the Cluster Configuration Manager. This \texttt{config.json} file is then decoded via the in-built terraform \texttt{jsondecode} function
to allow easy access to these variables in terraform code (Listing \ref{lst:json-tf}). 

\begin{lstlisting}[xleftmargin=10pt, caption={VPC module in \texttt{main.tf}}, label={lst:vpc-main}]
module "vpc" {
  source  = "terraform-aws-modules/vpc/aws"
  version = "5.8.1"

  name = "${local.config.prefix}-vpc"

  cidr            = "10.0.0.0/16"
  azs             = ["ap-southeast-1a", "ap-southeast-1b"]
  private_subnets = ["10.0.1.0/24", "10.0.2.0/24"]
  public_subnets  = ["10.0.4.0/24", "10.0.5.0/24"]

  map_public_ip_on_launch = true
  enable_nat_gateway      = true
  single_nat_gateway      = true
  enable_dns_hostnames    = true

  public_subnet_tags = {
    "kubernetes.io/role/elb" = 1
  }

  private_subnet_tags = {
    "kubernetes.io/role/internal-elb" = 1
  }
}

\end{lstlisting}

\begin{lstlisting}[xleftmargin=10pt, caption={Random string generation}, label={lst:random-str}]
resource "random_string" "suffix" {
  length  = 8
  special = false
}
\end{lstlisting}

Next, we create the EKS compliant VPC. The VPC module is souced from the hashicorp/aws provider, and uniquely identified with a 
name prepended by a prefix. This prefix is a concatenation of the user-provided prefix in step 2 of Figure \ref{fig:cloudkube-config},
as well as randomly generated string as shown in Listing \ref{lst:random-str}. This uniquely generated string allows users to easily identify
their resources in the cloud, and is especially useful for large teams where many resources are spun up within the cloud account. Furthermore,
some AWS resources also require unique names. The prepending of this unique string helps to avoid name collisions in the cloud.

The remainder of the code as shown in Listing \ref{lst:vpc-main} sets up 2 private subnets and 2 public subnets in separate Availability
Zones, as well as configures the relevant NAT Gateways for networking. Tags are also used for book keeping, as well as to identify
the public and private subnets to Kubernetes during the application of the Kubernetes manifest. 

\begin{lstlisting}[xleftmargin=10pt, caption={EKS Module in \texttt{main.tf}}, label={lst:eks-main}]
module "eks" {
  source  = "terraform-aws-modules/eks/aws"
  version = "20.8.5"

  cluster_name    = local.cluster_name
  cluster_version = "1.30"

  cluster_endpoint_public_access           = true
  enable_cluster_creator_admin_permissions = true
  cluster_enabled_log_types                = ["api", "audit", "authenticator", "controllerManager", "scheduler"]


  cluster_addons = {
    kube-proxy = {
      version = "v1.30.0-eksbuild.3"
    }
    coredns = {
      version = "v1.11.1-eksbuild.8"
    }
    vpc-cni = {
      version = "v1.18.1-eksbuild.3"
    }
    eks-pod-identity-agent = {
      version = "v1.3.2-eksbuild.2"
    }
  }

  vpc_id = module.vpc.vpc_id

  // Attach eks to all subnets in vpc (similar to amazon launch template for eks vpc)
  subnet_ids = module.vpc.private_subnets


  eks_managed_node_group_defaults = { # This should be user selected based on architecture type of docker images
    ami_type = local.config.architecture
  }

  # Extend node-to-node security group rules
  node_security_group_additional_rules = {
    ingress_self_all = {
      description = "Node to node all ports/protocols"
      protocol    = "-1"
      from_port   = 0
      to_port     = 0
      type        = "ingress"
      self        = true
    }
  }
  eks_managed_node_groups = {
    // name the nodes that are created
    one = {
      name = "node-group-1"

      instance_types = (
        local.config.architecture == "AL2_x86_64" ? ["t3.medium"] :
        local.config.architecture == "AL2_ARM_64" ? ["t4g.medium"] :
        []
      )

      min_size     = 2
      max_size     = 2
      desired_size = 2

      // We need to define additional iam policy so that node group can hit the s3 bucket
      iam_role_additional_policies = {
        "AmazonS3FullAccess" = "arn:aws:iam::aws:policy/AmazonS3FullAccess",
        "AmazonSSMManagedInstanceCore" : "arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore"
      }

    }
  }
}

\end{lstlisting}

Next, at the core of the infrastructure, we need to create the EKS module in the cloud. This module requires the greatest amount of
configuration, and the following will show what each of the above lines in Listing \ref{lst:eks-main} are responsible for. Detailed
explanations on the various configuration of the EKS module are available at the \href{https://registry.terraform.io/modules/terraform-aws-modules/eks/aws/20.33.1}{\color{blue}Hashicorp Documentation}.
\vspace{-1em}
\begin{itemize}
    \item \textbf{Line 2-10}: This sets up the minimal configuration necessary for the EKS module provided by \texttt{hashicorp/aws} provider.
    \item \textbf{Line 13-26}: This configures the relevant add-ons required for the Kubernetes cluster. This is equivalent to configuration of the add ons in Appendix \ref{appendix:aws-eks-addons}.
    The functionality of each of these add-ons are detailed in \ref{table:eks-addons}.
    \begin{table}[h]
        \centering
        \begin{tabular}{|l|p{10cm}|}
            \hline
            \textbf{Addon} & \textbf{Purpose} \\
            \hline
            kube-proxy (v1.30.0) & Maintains network rules on nodes for Kubernetes services. \\
            \hline
            CoreDNS (v1.11.1) & Handles DNS-based service discovery within the cluster. \\
            \hline
            VPC CNI (v1.18.1) & Manages networking for pods using the AWS VPC. \\
            \hline
            EKS Pod Identity Agent (v1.3.2) & Enables IAM authentication for pods to access AWS services securely. \\
            \hline
        \end{tabular}
        \caption{EKS Add-ons and Their Purposes}
        \label{table:eks-addons}
    \end{table}
    \vspace{-1em}
    \item \textbf{Line 34 - 62}: This configures a node group in the EKS, which represents a group of compute instances in the cluster. The architecture of these nodes (ie arm or x86) is decided here based on the user input when configuring the cluster in step 2 (Figure \ref{fig:cloudkube-init}). It should also be noted that the nodes within a Kubernetes cluster must be allowed to communicate with each other since the Kubernetes control plane pod may be deployed to any node. As such, in order to allow for commands to be sent between pods, all communication is allowed between nodes (Line 39 - 48).
    \item \textbf{Line 65 - 68}: Lastly, the SpeechLab application requires models to be pulled from an S3 bucket. As such, IAM policies are attached to the nodes within the cluster, to ensure that they are able to access the S3 bucket.
\end{itemize}

\subsection{Cluster Configuration Manager}
The Cluster Configuration Manager is an interactive tool designed to optimize the configuration of the Kubernetes cluster. It simplifies the process of defining essential configurations,
such as \textbf{region selection}, \textbf{resource name prefixes}, \textbf{machine architecture}, \textbf{ingress controllers}, and \textbf{persistent storage options}. Through an interactive
wizard that guides users through this setup, the Cluster Configuration Manager ensures consistency and ease of deployment. Once the configuration has been validated, the Cluster Configuration Manager
generates a \texttt{config.json} file, which serves as the core reference for the infrastructure provisioned. This allows users to efficiently deploy and manage Kubernetes clusters with minimal manual intervention. 

\begin{lstlisting}[xleftmargin=10pt, caption={Interactive cluster configuration wizard}, label={lst:configure-cluster-wizard}]

def configure_cluster_interactive():
"""
This function provides an interactive setup for the configuration of
the cluster.

It will then generate the `variables.tf` file, as well as a state file for
resources installed on the kubernetes cluster. Examples of such resources
include:
    - nginx-ingress-controller
    - efs-csi-driver
    ...
"""
    cont = input(
        "This will override any previous configuration set in config.json. Continue? [y/N]: "
    )
    if cont != "y":
        return

    config = {}

    # Configure Region Variable
    region = input("Select Region (ap-southeast-1): ")
    # strip whitespace
    region = "".join(region.split())
    if len(region) == 0:
        # use default ap-southeast-1
        config["region"] = "ap-southeast-1"
    else:
        config["region"] = region

    # Configure Prefix Variable
    prefix = input("Set prefix of all cloud resources: ")
    config["prefix"] = prefix

    # Configure Machine Architecture
    printb("Select the architecture of your machines: ")
    print("\t1. arm (uses AL2_ARM_64)")
    print("\t2. x86 (uses AL2_x86_64)")
    arch = input("Enter a number: ")
    if arch == "1":
        config["architecture"] = "AL2_ARM_64"
    elif arch == "2":
        config["architecture"] = "AL2_x86_64"

    # Configure ingress-controller
    printb("Select (if required), and ingress controller type: ")
    print("\t 1. None")
    print("\t 2. aws-nginx-ingress-controller")
    choice = input("Enter a number: ")
    if choice == "1":
        config["ingressController"] = "None"
    elif choice == "2":
        config["ingressController"] = "aws-nginx-ingress-controller"

    # Configure efs-csi-driver
    choice = input("Do you require persistent volume setup? [y/N]")
    if choice == "y":
        config["requireEFS"] = "True"
    else:
        config["requireEFS"] = "False"

    # Save the configuration
    with open("config.json", "w") as f:
        json.dump(config, f, indent=4)

    print("Configuration: \n", json.dumps(config, indent=4))

    printgreen("Configuration successfully saved to config.json!")
    print("[hint]: You can now spin up your cluster by selecting option 3.")

\end{lstlisting}

As seen from the above code snippet, the Cluster Configuration Manager does some simple string validation, and outputs a \texttt{config.json} file in the current working directory that 
\texttt{cloudkube} is run from. 

\subsection{Cluster Manager}
Lastly, the Cluster Manager is the main orchestrator in \texttt{cloudkube}. It is designed to handle provisioning and teardown of Kubernetes clusters in a structured and efficient manner.
The Cluster Manager ensures that the configuration file \texttt{config.json} is valid, and provisions the cluster with the predefined setting in \texttt{config.json}. By automating the provisioning and 
tear down workflow, the Cluster Manager reduces manual intervention, and provides a reliable, repeatable mechanism for deploying and managing Kubernetes clusters in the cloud. 

\begin{lstlisting}[xleftmargin=10pt, caption={Cluster Manager Provisioning and Teardown methods}, label={lst:cluster-manager}]
@staticmethod
def spin_up_cluster(path_to_config="config.json"):
    cfg = ClusterManager.__parse_config(path_to_config)
    if not cfg:
        print(
            "Invalid configuration file provided. Have you run '2. Configure cluster configuration?'"
        )
        return

    # Step 1: Apply Terraform
    cluster_name, region = apply_terraform()

    # Step 2: Configure kubectl for the EKS cluster
    configure_kubectl(cluster_name, region)

    # region, prefix, architecture are used in the main.tf terraform code

    # at this step, we only need to check ingress_controller and require_efs state

    if cfg["ingress_controller"] != "None":
        install_ingress_controller(cfg["ingress_controller"])

    if cfg["require_efs"] == "True":
        apply_efs_csi_driver(
            "kubernetes-sigs/aws-efs-csi-driver", ref="release-2.0"
        )

@staticmethod
def tear_down_cluster(path_to_config="config.json"):
    cfg = ClusterManager.__parse_config(path_to_config)
    if not cfg:
        print(
            "Invalid configuration file provided. Have you run '2. Configure cluster configuration?'"
        )
        return
    # User Prompt
    confirm = input(
        "Before tearing down the infrastructure, it is good practice to have removed all your kubernetes pods, services, and everything that was created using the kubernetes manifest. This is so as to ensure no dangling services on the cloud, such load balancers, which could cause infrastructure deprovisioning to fail. To confirm proceed, (y/N): "
    )

    if confirm == "y":

        # Clean up procedure
        # Order is important here, we are deleting in the reverse order that we created
        # create A -> create B -> create C -> destroy C -> destroy B -> destroy A
        # delete_efs_csi_driver("kubernetes-sigs/aws-efs-csi-driver", ref="release-2.0")
        if cfg["require_efs"] == "True":
            delete_efs_csi_driver(
                "kubernetes-sigs/aws-efs-csi-driver", ref="release-2.0"
            )
        if cfg["ingress_controller"] != "None":
            delete_aws_nginx_controller()
        unconfigure_current_kubeconfig()
        destroy_terraform()
        clean_local_config_json()

\end{lstlisting}

As seen in Listing \ref{lst:cluster-manager}, the infrastructure is provisioned and destroyed in a stack like manner. This ensures a consistent state within the infrastructure, and 
prevents dangling resources in the cloud. This is important because some resources have dependencies on previous resources. For example, \texttt{unconfigure\_current\_kubeconfig()} removes the
Kubernetes cluster context from the local machine. Doing this before removing the ingress controller will result in a dangling load balancer in the cloud, since we are now no longer able to 
interact with the Kubernetes cluster. As such, the deletion of resources must be in the reverse order that the resources were created. 

\section{Publishing and Distribution}
\texttt{cloudkube} reimagines the way developers work, particularly in smaller and more agile teams that lack a dedicated infrastructure team. Through consolidating infrastructure provisioning
into a streamlined workflow, \texttt{cloudkube} ensures that deployments are replicable, consistent, and easy to manage. This means that at its core, \texttt{cloudkube} must be versioned tagged, and be 
widely distributed to facilitate installation on any developers machine. In this research project, distribution was achieved via \texttt{PyPI}, the de facto public repository
of Python software packages. By leveraging \texttt{PyPI}'s extensive reach, \texttt{cloudkube} can be effortlessly installed, updated, and adopted within the Python developer community,
fostering wider accessibility and adoption. 

The workflow for packaging \texttt{cloudkube} is reliant on \texttt{setup.py} and \texttt{workflow.yml} (Appendix \ref{appendix:cloudkube-distribution}). The \texttt{setup.py} file is responsible for maintaining
the metadata of \texttt{cloudkube}, as well as tracking its dependencies for installation purposes. The \texttt{workflow.yml} file houses the GitHub Actions workflow for automating the publishing process. 
This ensures that every push to the main branch bumps the version number in \texttt{setup.py}, triggers a new release, thereby maintaining the latest version of \texttt{cloudkube} on \texttt{PyPI}. 